import {useEffect,useState}from'react';import SectionTitle from '../components/SectionTitle';import {getData,whatsapp}from'../api';const img='/assets/image-13.jpg';export default function Events(){const[events,setEvents]=useState([]);useEffect(()=>{getData('/events').then(setEvents)},[]);return <section className="section pt-32 bg-linen"><div className="max-w-7xl mx-auto px-5 grid lg:grid-cols-2 gap-12"><div><SectionTitle eyebrow="At The Gourmet Shop" title={`Events &<br/><em>Experiences</em>`}>Weekly cooking sessions, tasting evenings and monthly specials. Members get priority booking.</SectionTitle><img src={img} className="rounded-sm shadow-xl object-cover h-96 w-full"/></div><div className="grid gap-5 content-center">{events.map(e=><div key={e.id} className="card p-7"><span className="text-[.6rem] uppercase tracking-widest text-bordeaux">{e.badge}</span><h3 className="font-serif text-3xl text-olive-dark mt-2">{e.title}</h3><p className="text-stone-600 text-sm leading-7 mt-2">{e.description}</p><div className="flex items-center justify-between mt-5"><span className="font-semibold text-bordeaux">{e.price}</span><button className="btn-primary" onClick={()=>whatsapp(`Hi Steffi! I would like to book: ${e.title} (${e.price}). Please confirm my spot!`)}>Book</button></div></div>)}</div></div></section>}