import{useState}from'react';import{submitNewsletter}from'../api';export default function Newsletter(){const[email,setEmail]=useState('');const[ok,setOk]=useState(false);async function sub(e){e.preventDefault();await submitNewsletter(email);setOk(true)}return <section className="section bg-olive-dark text-white"><div className="max-w-3xl mx-auto px-5 text-center"><h2 className="font-serif text-4xl">Stay close to the kitchen</h2><p className="text-white/50 mt-3">Get updates about boxes, classes, events and loyalty benefits.</p>{ok?<p className="mt-6 text-orange-200">Thank you — you are subscribed!</p>:<form onSubmit={sub} className="mt-8 flex flex-col sm:flex-row gap-3"><input className="input text-ink" type="email" required placeholder="Your email" value={email} onChange={e=>setEmail(e.target.value)}/><button className="btn-primary">Subscribe</button></form>}</div></section>}